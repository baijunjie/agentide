{
  "name": "@agentide/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "@agentide/protocol": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false",
    "start": "node dist/index.js",
    "test": "node --test test/*.test.mjs"
  }
}
