# Agent Adapter 异步边界

- Agent SDK 以异步序列返回轮次时，启动失败可能在工厂调用时同步抛出，也可能到首次迭代才异步抛出；两条路径都要纳入轮次失败收口。
- 轮次启动前有恢复探测等可挂起操作时，必须在第一个 `await` 之前占用会话互斥状态，避免并发请求同时通过空闲检查而双启动。
- Agent SDK 的审批或提问回调可能在轮次取消后才到达；注册 abort listener 前必须先检查 `signal.aborted`，否则已取消的交互可能永久等待。
